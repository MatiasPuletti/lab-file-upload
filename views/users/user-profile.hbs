{{#if user}}
    <img src="{{user.imageUrl}}" alt="Profile Picture" style="height: 100px;">
    <h2>Welcome, {{ user.username }}!</h2>
    <p>This is your profile page! Your email is: {{ user.email }}</p>
{{else}}
    <p>Still no logged in, sorry!</p>
{{/if}}